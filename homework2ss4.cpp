#include<stdio.h> 
int main(){
	int number ; 
	printf("moi nhap so\n");
	scanf ("%d",&number) ;
	if (number %2==0) {
		printf("so nay la so chan");
		 
	}else 
	printf("so nay la so le");
	return 0;
	 
} 
