#include<stdio.h> 
int main(){
	int number;	
	printf("nhap so\n");
	 
	scanf("%d",&number) ;
	if (number >0){
		printf("so nay la so duong");
		 
	}else if (number <0) {
		printf("so nay la so am");
		 
	}else printf("so nay la so khong");

return 0 ;			 
}  
